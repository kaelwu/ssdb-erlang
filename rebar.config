{cover_enablel, true}.

{erl_opts, [{parse_transform, lager_transform}]}.

{cover_enabled, true}.

{deps,
    [
      {lager, ".", {git, "https://github.com/basho/lager", {branch, "master"}}}
    ]
}.

